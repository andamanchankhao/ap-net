# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Runtime output — regenerated by every run
edge_node/captures/
edge_node/local_sd_card/
edge_node/compressed_payload.txt
edge_node/compressed_preview.webp
base_station/received_images/
*.log

# Models are large binaries; export them per machine (see SETUP_PI.md).
# Matched anywhere: `yolo export` writes into whatever directory it runs from.
*.onnx
*.tflite
*.pt

# OS
.DS_Store
